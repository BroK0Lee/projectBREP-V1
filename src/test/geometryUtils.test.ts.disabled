// import { describe, it, expect } from 'vitest';
// import { mmToUnits, unitsToMm, calculerParametresChanfrein } from '@/utils/geometryUtils';

// describe('GeometryUtils', () => {
//   describe('conversions d\'unités', () => {
//     it('devrait convertir mm vers unités Three.js', () => {
//       expect(mmToUnits(1000)).toBe(1);
//       expect(mmToUnits(100)).toBe(0.1);
//       expect(mmToUnits(1)).toBe(0.001);
//     });

//     it('devrait convertir unités Three.js vers mm', () => {
//       expect(unitsToMm(1)).toBe(1000);
//       expect(unitsToMm(0.1)).toBe(100);
//       expect(unitsToMm(0.001)).toBe(1);
//     });
//   });

//   describe('calculs de chanfrein', () => {
//     it('devrait calculer les paramètres pour un chanfrein 45°', () => {
//       const params = calculerParametresChanfrein(2, 45);

//       expect(params.distance1).toBeCloseTo(2, 1);
//       expect(params.distance2).toBeCloseTo(2.83, 1);
//     });

//     it('devrait calculer les paramètres pour un chanfrein 30°', () => {
//       const params = calculerParametresChanfrein(3, 30);

//       expect(params.distance1).toBeCloseTo(10.39, 1);
//       expect(params.distance2).toBeCloseTo(6, 1);
//     });
//   });
// });
